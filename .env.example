### .env.example
# Modelo de variáveis de ambiente. Copie este arquivo para `.env.local` ou
# `.env` no seu ambiente local e preencha com as chaves reais. NÃO COMMITAR
# credenciais reais no repositório.

# URL pública do Supabase (projeto)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Chave anônima usada pelo cliente (você deve manter isso secreto no repositório)
VITE_SUPABASE_ANON_KEY=your-vite-supabase-anon-key

# URL do Supabase Admin/Service (opcional; use apenas em backend)
SUPABASE_URL=your-supabase-service-url
SUPABASE_DB_URL=postgres://user:password@host:port/dbname

# OBS: para operações administrativas (migrations, criação de buckets, etc.)
# use a Supabase CLI vinculada ao projeto (`supabase link`) ou variáveis de
# ambiente no CI com as chaves de service role.
